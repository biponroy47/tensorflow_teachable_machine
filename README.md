## Teachable Machine using Transfer Learning

Uses MobileNet v3 in TensorFlow.js and saved graph model from TFHub.
Clone repo, run 'http-server' command within project repo to run local Chrome development.
